# Story: Nix Runtime Installation

**STATUS: SUPERSEDED** — This story is replaced by [nix-native-container-image.md](nix-native-container-image.md).

The Nix-on-Debian installation approach (installing Nix inside a Debian container) has been replaced by a pure Nix container image where Nix is bundled natively via `dockerTools.buildLayeredImage`. See the nix-native-container-image story for the new approach.
